{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "microservices-web-vote-app.serviceAccountName" . }}
  labels:
{{ include "microservices-web-vote-app.labels" . | nindent 4 }}
{{- end -}}
